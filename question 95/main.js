// : Write a function that uses the .filter() method to return an array of numbers greater than 10.
let numbers = [12, 2, 11, 14, 15, 3, 6, 7, 16];
let filterd_Numbers = numbers.filter((numbers) => {
    return numbers > 10;
});
console.log(filterd_Numbers);
export {};
