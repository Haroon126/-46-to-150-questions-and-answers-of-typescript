// : Demonstrate the use of the setTimeout() function to execute code after a delay.

function delay() {
  setTimeout(() => {
    console.log("Hello World!");
  }, 2000);
}

delay();

//This is the setTimeout function is used for some delay ,The code will be execute after some delay
