// : Finding the Position of a Substring: Write a function that locates the first occurrence of the word "code" within any given string and returns its position.
function findingSubstring(str) {
    return str.indexOf("code");
}
let result = findingSubstring("I love to code, and I often read code written by others");
console.log(result); // found in 10 positon
export {};
