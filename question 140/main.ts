//: Explain the error that occurs when trying to use a reserved word as a variable name.

let let = 0;
console.log(let);

// SyntaxError: Unexpected strict mode reserved word
// we can not use reserve keyword as a variable
